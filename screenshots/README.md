# Screenshots

Drop 2-3 real screenshots here from an actual Claude session running the Extract and Verify prompts, this is what the work test explicitly asks for. Suggested shots:

1. The Extract prompt run against one real abstract, showing the frontmatter and table it produces.
2. The moment of catching one of the two errors during Verify, ideally the actual Claude conversation where you spot the dropped qualifier or mixed-up term.
3. The paper note inside Obsidian (or your notes app of choice), showing the `[[wikilinks]]` connecting it to the other two papers.

Reference them from the main [README](../README.md) once added.
